{
	"name": "satisplanner",
	"version": "0.5.0",
	"private": true,
	"type": "module",
	"packageManager": "pnpm@11.16.0",
	"engines": {
		"node": ">=24.0.0",
		"pnpm": "11.16.0"
	},
	"scripts": {
		"build": "pnpm -r --if-present build",
		"build:web": "pnpm --filter @satisplanner/desktop-ui build",
		"check:boundaries": "node scripts/check-boundaries.mjs",
		"check:game-assets": "node scripts/check-game-assets.mjs",
		"desktop:build": "tauri build --no-bundle",
		"desktop:dev": "tauri dev",
		"dev": "pnpm dev:web",
		"dev:web": "pnpm --filter @satisplanner/desktop-ui dev",
		"format": "biome format --write apps packages scripts playwright.config.ts package.json tsconfig.base.json",
		"format:check": "biome format --error-on-warnings apps packages scripts playwright.config.ts package.json tsconfig.base.json",
		"lint": "biome lint --error-on-warnings apps packages scripts playwright.config.ts",
		"quality": "pnpm format:check && pnpm lint && pnpm check:boundaries && pnpm check:game-assets && pnpm verify:quality-failures && pnpm typecheck && pnpm test && pnpm build:web",
		"test": "pnpm -r --if-present test",
		"test:e2e": "playwright test",
		"typecheck": "pnpm -r --if-present typecheck",
		"verify:quality-failures": "node scripts/verify-quality-failures.mjs"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.7",
		"@playwright/test": "1.62.1",
		"@tauri-apps/cli": "2.11.4",
		"@types/node": "24.10.13",
		"@types/react": "19.2.18",
		"@types/react-dom": "19.2.4",
		"@vitejs/plugin-react": "6.0.5",
		"typescript": "7.0.2",
		"vite": "8.2.1",
		"vitest": "4.1.10"
	}
}
