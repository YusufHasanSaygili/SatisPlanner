{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-default",
  "description": "Default-deny foundation capability: core window runtime only; no filesystem, shell, dialog, or network plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
